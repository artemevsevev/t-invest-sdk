[package]
name = "generator"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13.4"
prost-types = "0.13.4"
tonic = { version = "0.12.3", features = ["tls", "tls-roots", "gzip"] }
tonic-build = { version = "0.12.3", features = [] }
